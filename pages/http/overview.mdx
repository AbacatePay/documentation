---
title: 'Visão Geral'
icon: 'route'
description: 'Coleção oficial de requisições HTTP da AbacatePay para testar, integrar e entender a API sem abstrações.'
---

A **AbacatePay HTTP** é a coleção oficial de **requisições HTTP cruas** da AbacatePay, feita para quem quer **entender exatamente o que acontece na API**, sem camadas mágicas ou SDKs no meio.

Ideal para **debug**, **aprendizado**, **testes manuais** e **integrações customizadas**, usando arquivos `.http` executáveis diretamente na sua IDE ou cliente favorito.

---

## Principais Capacidades

<CardGroup cols={3}>
  <Card title="HTTP puro" icon="code">
    Requisições reais, sem abstrações ou helpers ocultos.
  </Card>

  <Card title="Versionado" icon="ellipsis">
    Exemplos separados por versão da API (v1 e v2).
  </Card>

  <Card title="Executável" icon="play">
    Rode requests direto do editor com um clique.
  </Card>

  <Card title="Webhooks" icon="webhook">
    Simulação local de eventos reais da plataforma.
  </Card>

  <Card title="Ambientes" icon="sliders">
    Variáveis via `.env` para alternar entre prod, sandbox e local.
  </Card>

  <Card title="Debug & Aprendizado" icon="bug">
    Veja headers, payloads e responses completos.
  </Card>
</CardGroup>

---

## Requisitos

Você pode usar qualquer ferramenta que suporte HTTP bruto com variáveis:

- **Insomnia**
- **VS Code** + extensão **REST Client**
- Qualquer cliente HTTP compatível com `.env`
- **JetBrains IDEs** (WebStorm, GoLand, IntelliJ, etc.)

---

## Configuração

Crie ou edite o arquivo de environment dentro da versão desejada.

Exemplo, `.env`

```env
API_VERSION=v1
WEBHOOK_SECRET=abc_my_secret
ABACATEPAY_API_KEY=abc_prod_xxx
BASE_URL=https://api.abacatepay.com
BASE_WEBHOOK_URL=http://localhost:3000
```

## Uso Básico

Abra qualquer arquivo `.http` e execute a requisição **diretamente pela sua IDE**.

Exemplo, criar um **QRCode PIX** na **v1**


```http
### Create QRCode PIX
POST {{$dotenv .env BASE_URL}}/{{$dotenv .env API_VERSION}}/pixQrCode/create

Content-Type: application/json
Authorization: Bearer {{$dotenv .env ABACATEPAY_API_KEY}}

{
  "amount": 1000
}
```

### Webhooks

Os arquivos `.http` também incluem exemplos completos para **simular webhooks localmente**, facilitando testes sem depender de eventos reais.

```http
### Simular webhook billing.paid
POST {{$dotenv .env BASE_WEBHOOK_URL}}/webhooks/abacatepay?webhookSecret={{$dotenv .env WEBHOOK_SECRET}}

X-Webhook-Signature: ...

{
  "id": "log_12345abcdef",
  "data": {
    "payment": {
      "amount": 1000,
      "fee": 80,
      "method": "PIX"
    },
    "pixQrCode": {
      "amount": 1000,
      "id": "pix_char_mXTWdj6sABWnc4uL2Rh1r6tb",
      "kind": "PIX",
      "status": "PAID"
    }
  },
  "devMode": false,
  "event": "billing.paid"
}
```

<CardGroup cols={2}>
    <Card title="SDK Oficial" icon="box" href="https://www.npmjs.com/package/@abacatepay/sdk">
        Wrapper de alto nível para produção. 
    </Card>
    <Card title="Documentação" icon="book" href="https://docs.abacatepay.com/pages/http">
        Referência completa da API.
    </Card>
    <Card title="REST Client" icon="repeat" href="../ecosystem/rest">
        Camada HTTP leve e tipada para integrações avançadas.
    </Card>
    <Card title="Ecossistema AbacatePay" icon="boxes-stacked" href="../ecosystem">
        Veja o ecossistema completo da AbacatePay.
    </Card>
</CardGroup>

<Card title="Open source de verdade" icon="heart">
    Mantido pela equipe AbacatePay e pela comunidade. Simples, transparente e sem mágica.
</Card>

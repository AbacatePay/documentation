---
title: 'Buscar um produto'
openapi: "GET /products/get"
---

Retorna os dados de um produto pelo seu `id`.

<Card horizontal>
  Requer a permissão `PRODUCT:READ`.
</Card>

Use para verificar `status`, `price`, `cycle` e demais atributos antes de criar um checkout com o produto.

---
title: 'Referência'
description: 'Gerencie seus clientes, aqueles que pagam você.'
icon: 'book-open-cover'
---

<div style={{ 
    position: "relative", 
    paddingBottom: "56.25%", 
    height: 0, 
    overflow: "hidden",
    maxWidth: "800px",
    margin: "32px auto",
    borderRadius: "8px"
}}>
<iframe
    src="https://www.youtube.com/embed/SEU_VIDEO_ID"
    title="Criar Cobrança com Checkout - AbacatePay"
    frameBorder="0"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
    allowFullScreen
    style={{ 
        position: "absolute", 
        top: 0, 
        left: 0, 
        width: "100%", 
        height: "100%" 
    }}
/>
</div>

## Criar cliente

Use `/customers/create` para criar um cliente antes da cobrança. Útil quando você já conhece os dados do cliente ou quer reutilizar o `customerId` em múltiplas cobranças.

<Card title="Campos obrigatórios" icon="info" horizontal>
  Apenas `email` é obrigatório. Recomendamos também enviar `name`, `cellphone` e `taxId` quando disponíveis.
</Card>

**Exemplo:**
```json
POST /customers/create
{
  "data": {
    "email": "customer@example.com",        // obrigatório
    "taxId": "12345678900",                 // opcional
    "name": "João Silva",                   // opcional
    "cellphone": "+5511999999999",          // opcional
    "zipCode": "01310-100"                  // opcional
  },
  "metadata": {                             // Aqui você pode adicionar dados adicionais da sua aplicação
    "customField": "value"
  }
}
```

**Resposta:**
```json
{
  "data": {
    "id": "cust_abc123xyz",
    "devMode": false,
    "email": "customer@example.com",
    "taxId": "12345678900",
    "country": "BR",
    "name": "João Silva",
    "cellphone": "+5511999999999",
    "zipCode": "01310-100",
    "metadata": {
      "customField": "value"
    }
  },
  "error": null
}
```

O `customerId` retornado pode ser usado ao criar cobranças, pré-preenchendo os dados do checkout.

**Alternativa**: O cliente também pode ser criado automaticamente ao criar uma cobrança via `/checkout/create` ou quando o cliente preenche os dados no checkout.

## Atributos

<AccordionGroup>
    <Accordion title="id:">
        ```json {2}
        {
          "id": "cust_abc123xyz",
        } 
        ```

        `id` : <u> string. </u> <br />
        Identificador único do cliente na AbacatePay
    </Accordion>
    <Accordion title="email:">
        ```json {2}
        {
          "email": "customer@example.com",
        }
        ```
        `email` : <u> string. </u> <br />
        E-mail do cliente (obrigatório)
    </Accordion>
    <Accordion title="name:">
        ```json {2}
        {
          "name": "João Silva",
        }
        ```
        `name` : <u> string. </u> <br />
        Nome completo do cliente (opcional)
    </Accordion>
    <Accordion title="cellphone:">
        ```json {2}
        {
          "cellphone": "+5511999999999",
        }
        ```
        `cellphone` : <u> string. </u> <br />
        Telefone do cliente (opcional)
    </Accordion>
    <Accordion title="taxId:">
        ```json {2}
        {
          "taxId": "12345678900",
        }
        ```
        `taxId` : <u> string. </u> <br />
        CPF ou CNPJ do cliente (opcional)
    </Accordion>
    <Accordion title="country:">
        ```json {2}
        {
          "country": "BR",
        }
        ```
        `country` : <u> string. </u> <br />
        País do cliente (opcional)
    </Accordion>
    <Accordion title="zipCode:">
        ```json {2}
        {
          "zipCode": "01310-100",
        }
        ```
        `zipCode` : <u> string. </u> <br />
        CEP do cliente (opcional)
    </Accordion>
    <Accordion title="devMode:">
        ```json {2}
        {
          "devMode": false,
        }
        ```
        `devMode` : <u> boolean. </u> <br />
        Indica se o cliente foi criado em ambiente de desenvolvimento (`true`) ou produção (`false`)
    </Accordion>
    <Accordion title="metadata:">
        ```json {2}
        {
          "metadata": {
            "customField": "value"
          },
        }
        ```
        `metadata` : <u> object. </u> <br />
        Objeto para armazenar informações adicionais sobre o cliente (opcional)
    </Accordion>
</AccordionGroup>

<Tip title="Clientes são únicos por CPF/CNPJ" icon="book" horizontal>
 Não é possível criar um cliente com CPF/CNPJ inválido e caso já exista um cliente com esse CPF/CNPJ, a API não criará o cliente novo mas retornará o cliente já existente
</Tip>
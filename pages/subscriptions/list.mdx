---
title: 'Listar Checkouts de assinatura'
openapi: "GET /subscriptions/list"
---

Lista os checkouts de assinatura. Cada item em `data` tem o mesmo formato do Checkout (`id`, `url`, `amount`, `items`, `status`, etc.). Pagine com `limit` e `cursor`.
